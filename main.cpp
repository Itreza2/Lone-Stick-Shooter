#include <iostream>

#include "Window.h"
#include "Keyboard.h"

int main(int argc, char** argv)
{
	Window window = Window();

	bool exit = false;
	while (!exit) {
		//Tt's quite empty in here...
	}
	return EXIT_SUCCESS;
}